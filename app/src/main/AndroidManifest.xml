<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jhorje18.quicktrade">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/carrito"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/carrito"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".NuevoUser"
            android:label="Añadir usuario"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".EditarUser"
            android:label="Editar usuario"
            android:parentActivityName=".NuevoUser" />
        <activity
            android:name=".MostrarUsers"
            android:label="Lista usuarios registrados"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".Login"
            android:label="Iniciar sesión"/>
        <activity android:name=".Registro"
            android:label="Crear cuenta"></activity>
    </application>

</manifest>